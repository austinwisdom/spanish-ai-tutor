import React from 'react';

const Conversation = () => {
    return (
        <section className='w-full h-screen relative'>
            <h1>CONVERSATION</h1>
        </section>
    );
};

export default Conversation;